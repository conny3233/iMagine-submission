# 기본: 텍스트는 자동 정규화, 저장소에는 LF
* text=auto eol=lf

# 스크립트는 항상 LF
*.sh   text eol=lf
*.ps1  text eol=crlf

# 바이너리 (diff/merge 안 함)
*.pdf   binary
*.hwpx  binary
*.png   binary
*.jpg   binary
*.ico   binary
